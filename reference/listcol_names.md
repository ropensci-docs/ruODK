# A functional to extract names of list columns from a tibble.

**\[stable\]**

## Usage

``` r
listcol_names(tbl)
```

## Arguments

- tbl:

  A tibble, possibly with list columns

## Value

A vector of list column names
